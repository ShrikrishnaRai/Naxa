<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical"
    tools:context="naxa.naxaco.com.naxa.MyProfile.MyProfileDisplay">

    <ImageView
        android:id="@+id/full_display_my_profile_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageButton
        android:id="@+id/share_button"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:tint="@color/white"
        android:layout_above="@+id/share_text"
        android:layout_alignParentStart="true"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_share_black_24dp" />

    <TextView
        android:id="@+id/share_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/detail_text"
        android:layout_alignBottom="@+id/detail_text"
        android:layout_alignStart="@+id/detail_button"
        android:gravity="center_horizontal"
        android:text="    Detail"
        android:textColor="@color/white" />

    <ImageButton
        android:id="@+id/detail_button"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/share_button"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_info_black_24dp"
        android:tint="@color/white" />

    <TextView
        android:textColor="@color/white"
        android:gravity="center_horizontal"
        android:id="@+id/detail_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/share"
        android:text="    Share" />

</RelativeLayout>
